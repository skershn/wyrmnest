<br />

<%= simple_form_for ([@topic, @comment]) do |f| %>
  <%= f.input :comment, label: false, label_html: { class: "commentbox" }, placeholder: "add a comment" %>
  <br />
  <%= f.submit 'update', class: 'btn btn-info btn-block' %>
<% end %>