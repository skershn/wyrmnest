<%= simple_form_for ([@topic, @topic.comments.create]) do |f| %>
  <%= f.input :comment, label: false, placeholder: "add a comment..." %>
  <br />
  <%= f.submit 'post', class: 'btn btn-success btn-block' %>
<% end %>
